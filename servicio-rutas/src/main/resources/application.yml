server:
  port: 8081 # Puerto en el que correrá este microservicio

spring:
  application:
    name: servicio-rutas # Nombre del servicio que se registrará en Eureka

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/ # URL del Eureka Server